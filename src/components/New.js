import React from 'react'
import { useState } from 'react'

const New = () => {
        //variables
        

        

        const [Nume, setNum] = useState()
        
        /*var dnum2 = num[Math.floor(Math.random()*num.length)];
    
        var cnum = num[Math.floor(Math.random()*num.length)];
        var cnum2 = num[Math.floor(Math.random()*num.length)];
    
        const [totalUno, setNumbers] = useState(dnum+dnum2)
        
        
        var compuT = cnum + cnum2;
        
    
        
    console.log('works')
    var dnum3 = num[Math.floor(Math.random()*num.length)];
    console.log(dnum3)*/
    
    return (
        <div>
            <h1>Your new number is: {}</h1>
        </div>
    )
}

export default New

